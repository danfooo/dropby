# Archive

Kept for reference; nothing here is used by the app or maintained.

- `specs/` — early specs from March 2026. `spec-full.md` at the repo root is the source of truth.
- `icons/` — app icon explorations (stroke-width and composition variants, the editor and preview pages that compare them, and early house drafts). The icons in use are `ios-icon.svg`, `logo.svg` and `ic_launcher_foreground.svg` at the repo root.
