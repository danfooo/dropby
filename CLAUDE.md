Project instructions live in `AGENTS.md`, shared with other coding agents. The import below makes Claude Code load it in every session (it reads `AGENTS.md` on its own only when no `CLAUDE.md` exists, and only on v2.1.277+).

@AGENTS.md
